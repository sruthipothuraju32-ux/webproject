# ===============================
# Unemployment Analysis Project
# ===============================

# 1. Import Required Libraries
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns
from sklearn.linear_model import LinearRegression
from sklearn.metrics import mean_squared_error
from statsmodels.tsa.arima.model import ARIMA
import warnings
warnings.filterwarnings("ignore")

# 2. Load Dataset
df = pd.read_csv("unemployment_data.csv")

# 3. Data Preprocessing
df['Date'] = pd.to_datetime(df['Date'])
df = df.sort_values('Date')
df = df.dropna()

print("Dataset Preview:")
print(df.head())

print("\nDataset Info:")
print(df.info())

# 4. Basic Statistics
print("\nStatistical Summary:")
print(df.describe())

# 5. Visualization - Unemployment Trend Over Time
plt.figure(figsize=(12,6))
plt.plot(df['Date'], df['Unemployment_Rate'], color='blue')
plt.title("Unemployment Rate Over Time")
plt.xlabel("Date")
plt.ylabel("Unemployment Rate (%)")
plt.grid(True)
plt.show()

# 6. Regional Comparison (if multiple regions exist)
if 'Region' in df.columns:
    plt.figure(figsize=(10,6))
    sns.barplot(x='Region', y='Unemployment_Rate', data=df)
    plt.title("Unemployment Rate by Region")
    plt.xticks(rotation=45)
    plt.show()

# 7. Correlation Heatmap (if more numerical columns exist)
plt.figure(figsize=(8,6))
sns.heatmap(df.corr(numeric_only=True), annot=True, cmap='coolwarm')
plt.title("Correlation Matrix")
plt.show()

# 8. Linear Regression Prediction
df['Time_Index'] = np.arange(len(df))

X = df[['Time_Index']]
y = df['Unemployment_Rate']

model = LinearRegression()
model.fit(X, y)

df['Predicted_Rate'] = model.predict(X)

plt.figure(figsize=(12,6))
plt.plot(df['Date'], y, label='Actual')
plt.plot(df['Date'], df['Predicted_Rate'], label='Predicted', linestyle='--')
plt.legend()
plt.title("Linear Regression Prediction")
plt.show()

# 9. ARIMA Time Series Forecasting
series = df.set_index('Date')['Unemployment_Rate']

model_arima = ARIMA(series, order=(2,1,2))
model_arima_fit = model_arima.fit()

forecast = model_arima_fit.forecast(steps=12)

plt.figure(figsize=(12,6))
plt.plot(series, label='Actual')
plt.plot(forecast.index, forecast, label='Forecast', color='red')
plt.legend()
plt.title("ARIMA Forecast (Next 12 Periods)")
plt.show()

print("\nForecasted Values:")
print(forecast)

# 10. Model Evaluation
mse = mean_squared_error(y, df['Predicted_Rate'])
print("\nLinear Regression MSE:", mse)

print("\nProject Completed Successfully!")vv